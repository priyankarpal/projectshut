name: Greetings

on: [pull_request_target, issues]

jobs:
  greeting:
    runs-on: ubuntu-latest
    permissions:
      issues: write
      pull-requests: write
    steps:
    - uses: actions/first-interaction@v1
      with:
        repo-token: ${{ secrets.GITHUB_TOKEN }}
        issue-message: "Thanks for creating your first issue in ProjectsHut 🎉 ![Awesome](https://user-images.githubusercontent.com/88102392/223393520-c2d9061c-14ce-4c36-9713-bc661d579eed.png) "
        pr-message: "Awesome you created your first PR in ProjectsHut 🎉"
